# Installer

`install.sh` runs inside the Arch live ISO, wipes the target disk, installs a
minimal Arch system plus the `gameos-*` packages, and enables `gameos.service`
for first boot.

This branch does not pre-seed any game account credentials. The appliance boots
straight into the local Minecraft chooser.

# systemd glue

Canonical installed names on this branch:

- `gameos.service`
- `gameos-performance.service`
- `gameos-session`
- `gameos-performance`

Cheap compatibility aliases for the old `factorios-*` names are installed where
practical, but the neutral `gameos-*` names are the intended surface.

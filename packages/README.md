# Arch packages

Current package outputs:

- `gameos-launcher`
- `gameos-greeter`
- `gameos-base`

The PKGBUILD directories still live under `packages/factorios-*` for repo
continuity, but the produced package names are the neutral `gameos-*` names.

Build through the top-level script:

```bash
./build.sh
```
